%{
#include <stdio.h>
%}

%%
"if"|"else"|"while"|"for"|"int"|"float"|"char"|"return" {
    printf("%s : Keyword\n", yytext);
}

[A-Za-z][A-Za-z0-9]* {
    printf("%s : Identifier\n", yytext);
}

[ \t\n]+ ;

. {
    printf("%s : Invalid Token\n", yytext);
}
%%

int yywrap()
{
    return 1;
}

int main()
{
    printf("Enter the input:\n");
    yylex();
    return 0;
}