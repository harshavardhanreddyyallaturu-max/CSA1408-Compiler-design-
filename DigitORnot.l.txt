%{
#include <stdio.h>
%}

%%
[0-9]+      { printf("It is a Digit (Number)\n"); }

.*          { printf("It is Not a Digit\n"); }
%%

int yywrap()
{
    return 1;
}

int main()
{
    printf("Enter the input: ");
    yylex();
    return 0;
}